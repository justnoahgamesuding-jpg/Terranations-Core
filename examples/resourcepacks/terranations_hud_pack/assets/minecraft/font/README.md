`zephyreg.ttf` is wired through `default.json`.

Overriding `assets/minecraft/font/default.json` makes Minecraft use that font for default text, including the scoreboard, tab list, chat, and menus for players using the resource pack.
